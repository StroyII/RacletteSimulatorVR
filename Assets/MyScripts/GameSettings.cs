using UnityEngine;

// Static class to hold game settings
public static class GameSettings
{
    // Movement type setting
    public static int movementType = 0; // 0 = teleportation, 1 = continuous
}
